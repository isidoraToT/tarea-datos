#include <iostream>
#include <fstream>
#include <string>
#include <math.h>

using namespace std;

int main() {
    
// Declaración de variables
string texto, linea, nombre, apellido, edad, salida;
// Archivo
fstream prueba;
//tenemos que asegurarnos que el archivo exista, por lo tanto si no existe lo creamos
prueba.open("documento.txt");
if(!prueba.is_open){
//si no se abre es por que no existe y debemos crear un archivo


}








    return 0;
}
